spring.application.name=map

#MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/post?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=a12752411!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf
spring.thymeleaf.check-template-location=true

# Open-in-view (Disable lazy-loading during view rendering for better performance)
spring.jpa.open-in-view=false

# ?? ??? ?? ?? (10MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
